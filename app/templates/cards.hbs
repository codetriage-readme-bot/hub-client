<div class="row">
  <div class="col-md-3">
    {{layout/side-bar}}
  </div>
  <div class="col-md-9">
    {{#each cards as |card|}}
      {{cards/cards-container card=card}}
    {{else}}
      Sorry, nobody is here.
    {{/each}}
  </div>
</div>
