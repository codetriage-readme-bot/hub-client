<div class="card">
  <div class="card-block">
    <h4 class="card-title">{{card.title}}</h4>
    <p class="card-text">{{card.description}}</p>
  </div>
</div>

{{#link-to 'card.card.edit' card}}Edit{{/link-to}}

{{outlet}}
